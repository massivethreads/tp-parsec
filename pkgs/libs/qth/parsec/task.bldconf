#!/bin/bash

if [ ${platform} == "g" ]; then
	# load gcc.bldconf
	source ${PARSECDIR}/pkgs/libs/qth/parsec/gcc.bldconf
else
	# load icc.bldconf
	source ${PARSECDIR}/pkgs/libs/qth/parsec/icc.bldconf
fi
