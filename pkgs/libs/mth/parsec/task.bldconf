#!/bin/bash

if [ ${platform} == "g" ]; then
	# load gcc.bldconf
	source ${PARSECDIR}/pkgs/libs/mth/parsec/gcc.bldconf
else
	# load icc.bldconf
	source ${PARSECDIR}/pkgs/libs/mth/parsec/icc.bldconf
fi
