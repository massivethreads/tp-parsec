#!/bin/bash

if [ ${platform} == "g" ]; then
	# load gcc.bldconf
	source ${PARSECDIR}/pkgs/libs/uptcpip/parsec/gcc.bldconf
else
	# load icc.bldconf
	source ${PARSECDIR}/pkgs/libs/uptcpip/parsec/icc.bldconf
fi
