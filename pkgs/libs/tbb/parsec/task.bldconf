#!/bin/bash

if [ ${platform} == "g" ]; then
	# load gcc.bldconf
	source ${PARSECDIR}/pkgs/libs/tbb/parsec/gcc.bldconf
else
	# load icc.bldconf
	source ${PARSECDIR}/pkgs/libs/tbb/parsec/icc.bldconf
fi
