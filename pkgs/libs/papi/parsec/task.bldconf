#!/bin/bash

if [ ${platform} == "g" ]; then
	# load gcc.bldconf
	source ${PARSECDIR}/pkgs/libs/papi/parsec/gcc.bldconf
else
	# load icc.bldconf
	source ${PARSECDIR}/pkgs/libs/papi/parsec/icc.bldconf
fi
