#!/bin/bash

# icc-tbb.bldconf - configuration file for PARSEC

source ${PARSECDIR}/pkgs/tools/libtool/parsec/icc.bldconf

# Package dependencies
# NOTE: libtool itself does not depend on tbb but the
# library is added globally and hence we must add it
build_deps="tbblib"

